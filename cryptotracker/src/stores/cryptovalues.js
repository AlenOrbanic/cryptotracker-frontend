const useremail = localStorage.getItem('useremail') || null;
const cryptovalues = {
    btc: 0,
    ethereum: 0,
    ripple: 0,
    litecoin: 0,
    bitcoinDash: 0,
    useremail
}

export default cryptovalues;